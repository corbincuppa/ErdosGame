#ifndef WRITE_GRAPH6
#define WRITE_GRAPH6

#include "../bitset.h"

//	Loads a graph in graph6 format into an adjacencylist representation
//	consisting of a list of bitsets.
char* writeGraph(int numberOfVertices, bitset
adjacencyList[]);

#endif